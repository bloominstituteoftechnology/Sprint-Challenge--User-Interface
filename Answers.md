1. If you had to describe semantic HTML to the next cohort of students, what would you say?
    (Semantic HTML is a way to allow other developers to understand the code you writing easier and faster than previously. It also helps with SEO practices.)
2. Describe some differences between ```display: block;``` and ```display: inline;```.
    ( ```display: inline;``` allows multiple elements to share the same line. It also doesn't let you change the height and width of the object. ```display: block;``` and makes objects take up the entire line and doesn't allow them to share the same line. Unlike the previous display property ```display:block;``` lets you change the height and width of the object. )
3. What are the 4 areas of the box model?
    (Content, Padding, Border, and Margin.)
4. While using flexbox, what axis are you using when you use the property: ```align-items: center```?
    (The cross axis.)
6. What is the git command to commit staged changes as well as write a message?
    (git commit -m"A beautiful message!")